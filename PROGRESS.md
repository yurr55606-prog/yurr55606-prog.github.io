# 当前开发进度

更新时间：2026-07-04

## 已完成

- 黑洞入口、穿越转场和中文个人作品入口。
- SDF 光线步进五维空间，多轴递归书架与局部多消失点透视。
- 程序化书脊、三平面深色木纹、铜银反射、体积雾与时间薄片。
- 鼠标视差、滚轮穿越不同时间切片。
- 第一人称随机眨眼：进入空间后每隔 5～10 秒触发一次 240ms 快速黑屏；作品详情打开时暂停。
- 五维空间内不放人物。

## 最近验证

- `npm run build` 通过。
- `git diff --check` 通过。
- 本地地址：`http://127.0.0.1:5173/`
- 浏览器控制台无站点运行错误。
- 随机眨眼回归：进入空间后 15.8 秒记录到 2 次触发，符合每次间隔 5～10 秒；当前仍在空间且无详情面板遮挡。

## 最新光色量化

- 当前平均亮度约 `0.233`，目标约 `0.21–0.22`。
- 当前高光 P90 约 `0.422`，目标约 `0.45–0.52`。
- 当前色温差约 `0.011`，已由偏青恢复为轻微暖中性，冷色仍为主调。
- 当前 P97 高光约 `0.545`，亮部集中在梁、书架边缘和反射光带。
- 几何、光色、随机眨眼和滚轮穿行均已完成本轮回归。
- 新增只作用于 WebGL 场景的五采样方向性电影拖影；高频二阶噪声降低约 `13.5%`，主轮廓梯度仅下降约 `3%`。
- 静止强度调整为桌面 `0.42px`、移动端 `0.28px`；量化发现目标图的高频能量更高，因此静止时优先保留锐利书架纹理。
- 方向拖影已接入鼠标视线与滚轮冲量：滚动时最多额外增加约 `1.35px`，随后按 `3.4` 阻尼快速恢复静止清晰度。
- 最终入口审查：`插件`、`摄影作品`、`视频作品` 均在浏览器中实际打开正确中文详情并成功返回。
- 最终禁止项审查：空间内无人物模型/轮廓、无星空背景对象。
- 最终视觉审查：X/Y/Z 多轴分支、宏观框架、局部多消失点、时间薄片、滚轮深度和第一人称随机眨眼均已保留。

## 首页黑洞电影化重制（2026-07-04）

- 保持标准网页全视口响应式尺寸，没有锁定为参考图分辨率。
- 宽屏采用右侧巨大事件视界、左侧暗部留白的电影构图；窄屏会自动回收主体避免完全裁切。
- 吸积盘由单薄橙色光带改为暗铜、琥珀与白热内缘组成的厚重物质流，包含多尺度云雾、颗粒与长曝光纹理。
- 引力透镜不再绘制完整奶白椭圆，改为以上方为主、下方极弱的非闭合分层光流。
- 光子环已收窄并紧贴事件视界，前景盘降低过曝，保留纯黑视界主体。
- 已在 1280×720 实际浏览器视口复核构图，`npm run build` 通过。

### 电影纹理混合渲染

- 新增原创电影级底层纹理：`public/assets/blackhole-cinematic-v1.png`（1672×941，约 1.7 MB）。
- 生成要求：右侧巨大事件视界、左侧深黑留白、暗铜与象牙金吸积盘、白热连接点、上方引力透镜弧；禁止人物、飞船、文字、水印、星云与土星式平面圆环。
- `blackHole.js` 已由纯程序几何渲染改为“摄影纹理 + WebGL 动态”混合方案：亚像素云雾流动、分层采样、受物质遮罩约束的粒子、鼠标视差、穿越吸入与放射光丝。
- 响应式裁切使用 cover 逻辑；宽屏将黑洞压在右缘，窄屏从右侧取景，保证事件视界不会被裁出画面。
- 桌面端 1280×720 与移动端 390×844 均已实拍复核；画布尺寸与视口一致。
- 间隔 1.8 秒的双帧截图内容不同，动态流动持续运行；入口点击后 4.4 秒正确进入五维空间。
- 浏览器页面无站点运行错误；外部浏览器遥测曾出现一次网络超时，不影响本地页面。

### 可见光云流动修正（2026-07-04）

- 撤销横向刻度光束、离散环形亮点和周期性发光脉冲，恢复干净的电影纹理。
- 下方吸积盘改为沿切线方向连续平流：双纹理相位交叉淡化，循环无跳帧。
- 上方光云改为围绕事件视界中心旋转平流，形成弯曲绕行的引力透镜物质流。
- 流动遮罩排除纯黑视界与白热光子环，只让真实的铜金物质和云雾运动。
- 中间调曝光与边缘亮度提高，保留纯黑背景并改善此前偏暗问题。
- 已用 1280×720 相隔 1 秒的双帧实拍确认盘面和上方光云均发生连续位移。
- 已按用户截图接近的 690×550 视口复核，画面无横向刻度或怪异光点。

### 体积光、粒子与宽光带增强（2026-07-04）

- 新增两层连续翻卷的体积光云，与盘面平流和上方弧形平流保持不同速度。
- 新增三层柔光粒子：随机尺寸、随机位置、极短拖尾，只在铜金物质与光云内部出现。
- 白热区增加八方向多采样柔光，事件视界内部使用椭圆遮罩继续保持纯黑。
- 撤销规则椭圆生成的三层光带，最终改为从原始白热边缘进行多尺度柔光扩散，避免同心圆与悬空光环。
- 中间调曝光、香槟金色温与边缘亮度均已提高，改善暗淡和偏红问题。
- 修复纹理右边界采样畸变：响应式取景保留安全裁切区，动态遮罩在边缘平滑关闭。
- 光晕附着不再使用估算椭圆，而是直接读取原图边缘亮度；光带始终贴合真实事件视界。
- 已在 320×844 极窄屏确认无右侧横向拉伸；690×550 视口确认宽光带、体积光和自然光粒子。

### 双参考约束重制（2026-07-04 晚）

- 新增 `public/assets/blackhole-cinematic-v4.png`：图一约束圆形事件视界、前后遮挡和连续厚环，图二约束象牙金/铜色材质、粒子光流和宏大尺度。
- 删除会造成视界内假横线、规则栅格和白色厚墙的程序化补画；事件视界、光子环、前景盘与下方透镜现在由同一电影底片保持一致几何。
- 黑洞中心统一为底片坐标 `(0.598, 0.500)`，静态流动、鼠标视差和点击穿越都围绕同一个中心缩放，不再横向换轴。
- 穿越阶段延后五维空间显露，先完成事件视界吸入；规则放射辐条已改为受噪声控制的少量断续光丝。
- 黑洞专用曝光降至 `0.96`，减少纯白过曝并恢复象牙金、铜金和深黑的层次。
- 1280×720 浏览器实拍确认：完整圆形、贴边细光子环、连续上下透镜环、厚暗前景吸积盘均无分割线或边缘脱离。

### v5 圆度、厚光圈与同心转场（2026-07-04）

- 新增 `public/assets/blackhole-cinematic-v5.png`：相较 v4，前景暗盘缩薄并下移，外部白热—象牙金透镜环加厚，铜金盘面纹理更连续。
- 新底片实际事件视界中心校准为 `(0.587, 0.505)`；桌面取景、窄屏取景、着色器缩放中心与空间揭示原点已统一。
- 事件视界半径采用数学圆形遮罩 `0.207`，黑色核心不再受流动噪声影响；前景吸积盘仍保留真实遮挡关系。
- 动态噪声位移从事件视界及贴边光子环移除，只作用于半径更外侧的光云、粒子和盘面物质。
- 五维空间以同中心圆形裁切揭示；入场相机先锁正轴，走廊旋转和漂移在转场尾段逐步恢复，避免消失点突然偏向右下。
- 桌面 `1280×720` 与窄屏 `390×844` 浏览器实拍确认事件视界保持圆形；右侧纹理钳制条纹已消除。

### v8 光流补全与性能优化（2026-07-04）

- 新增 `public/assets/blackhole-cinematic-v8.png`：左下光体积收窄，事件视界与外部主光圈之间的暗色空环改为连续象牙白—香槟金流光纤维。
- 前景吸积盘在事件视界内保持连续遮挡，光子环不再穿透盘面形成竖缝或矩形断层。
- 黑洞动态噪声只作用于外层物质；基础 fBm 从 6 层降至 5 层，柔光采样从 8 次降至 4 次，电影底片细节保持不变。
- 首页不再后台持续渲染五维空间；空间仅在转场后半段和正式进入后开始绘制。
- 黑洞 DPR 上限调整为桌面 `1.45` / 窄屏 `1.15`；五维空间独立调整为桌面 `0.64` / 窄屏 `0.56`，页面 CSS 尺寸不变。
- 五维空间最大 SDF 步进由 112 降至桌面 56 / 窄屏 44；软阴影 24→10 次、AO 5→3 次，并移除高成本 Afterimage 全屏后处理。
- 浏览器 1280×720 实测：黑洞首页 `60 FPS`，五维空间由约 `19 FPS` 提升到约 `54 FPS`。
- 浏览器 390×844 实测：黑洞首页与五维空间均约 `60 FPS`；WebGL fallback 保持隐藏，两个画布正常运行。

### 定向光流（2026-07-04）

- 参考红线拆分为两套速度场：事件视界外圈逆时针环流；前景吸积盘沿斜向切线从左向右流动。
- 借鉴 `ebruneton/black_hole_shader` 的极坐标吸积盘思路与 OGL Flowmap 的 UV 平流方式，直接集成进现有 GLSL，没有新增运行时依赖。
- 外圈采样改为经过宽高比校正的极坐标旋转，避免圆形光圈在流动时被拉成椭圆。
- 前景盘与外圈分别使用独立速度、遮罩与双相位交叉采样；前景盘始终遮挡后方光圈。
- 增加少量方向性亮团：外圈沿逆时针移动，盘面沿左→右移动，使流向在静态纹理之外也能被感知。
- 定向流加入后桌面黑洞首页仍实测约 `60 FPS`；390×844 窄屏无边缘拉伸或圆度变化。

### 事件视界内缘补光与断层修复（2026-07-05）

- 红圈内暗部改为两层动态介质：完整贴合圆形内缘的低亮暖光，以及不同速度绕行的细密光云；深黑核心继续保留。
- 前景吸积盘的纹理平流限制在事件视界之外，消除右侧灰白纹理被硬拖入黑洞的接缝。
- 内缘光流与外层光云之间改为单次径向渐隐，并加入不规则金白过渡光，修复左侧可见暗缝与材质断层。
- 二次精查确认断层实际位于细光子环外侧：现从每个像素自身半径向外偏移取样动态光云，再以宽羽化折回空环；不再使用近似固定半径，避免生成灰圈或第二圆环。
- 连续帧测量：内缘光流区域约 `68%` 像素发生可见变化，前景盘约 `57%`；不是静态贴图或整体平移。
- 当前正常网页尺寸及 `390×844` 窄屏均已复核，事件视界保持圆形；首页保持约 `60 FPS`。

### 中部暗盘光流与左右断层终修（2026-07-05）

- 对照底片确认原始光云没有断层；删除会生成棕色月牙夹层的空环重采样、核心清理和整圈内缘补光。
- 光云纹理旋转起点外移到半径 `0.285` 之外；光子环附近恢复底片原生纤维，只以二维无接缝噪声叠加亮度流动。
- 盘面遮挡改为低频噪声扰动的宽羽化过渡，左右汇聚处不再沿直线突然切断。
- 黑洞前方暗盘新增独立程序化流丝：细纹限定在亮边下方的盘体内部，从左向右输送，不平移底片、不侵入黑色核心。
- 相隔约 `420ms` 的连续帧验证：暗盘中心约 `37%` 像素发生可见变化；核心上方仅约 `9%`，证明运动集中在盘面。
- 桌面 `1280×720` 与移动端 `390×844` 复核均保持圆形与连续光层，首页实测约 `60 FPS`。

### 全光圈逆时针差速流场（2026-07-05）

- 参考 `bzk9x/blackhole` 的“极角 + 对数半径 + 时间”螺旋发射纹，以及 `Scenes3D/black-hole` 的多层差速吸积盘方法，重写完整光圈的流动模型。
- 光圈不再旋转或扭曲整张底图，而是在保持圆形几何与原生光子环的前提下叠加三层连续光流：内层更快、外层更慢。
- 整体方向统一为逆时针：右侧向上、顶部向左、左侧向下、底部向右；前景暗盘仍独立从左向右流动。
- 三层螺旋频率均使用整数角频率，消除极角 `-PI / PI` 交界处的可见断层；光流仅调制暖色物质，不侵入纯黑事件视界。
- 光圈靠近视界的区域恢复 v8 底片原始纹理，外层采样旋转延后到半径 `0.285` 之外，避免边缘褐色夹层与圆度变化。
- 移动端 `390×844` 运行时复核：两个 WebGL 画布均完整覆盖 CSS 视口，控制台无站点错误；生产构建与 `git diff --check` 均通过。

### 摄影作品双相册与折叠画廊（2026-07-06）

- 将“真实图片”46 张与“AI 图片”8 张整理为摄影作品内的两个可点击相册；相册封面直接以三张照片拼接呈现，不展示源文件名。
- 原始真实照片总量约 135MB，已生成最长边 1800px 的网页展示版；两组网页素材合计约 17MB，并对画廊图片启用懒加载与异步解码。
- 相册内部改为纵向滚动吸附的 3D 折叠胶片：向上滑动切换照片，前后图片沿 X 轴折叠并退入纵深，中心图保持清晰。
- 中心选中照片与普通排序照片的浏览器实测宽度比为桌面 `1.51`、移动端 `1.50`，满足放大一半的要求。
- 竖图与横图均使用“同图柔焦环境光背景 + 清晰原图”双层展示，消除生硬黑边，同时保留完整构图。
- 视觉采用深色玻璃、极细网格、冷银光、圆角照片与低饱和环境光，结合 INS 图像展示和 Apple 广告式克制动效。
- 交互参考 Codrops 的 3D Folding Layout 思路，并使用原生 CSS Scroll Snap、Perspective 与少量 JavaScript 距离计算完成；未复制第三方代码、未新增运行时依赖。
- 浏览器实测通过：首页进入、摄影入口、两个相册打开、上滑从 `01` 切到 `02`、返回相册均正常；桌面与 `390×844` 移动端控制台无错误。

### 五个插件产品案例（2026-07-06）

- 插件区由三条占位文字重构为五个真实产品案例：BlobstoreKey 自动采集助手、视界标注·图片数据工作台、视频批量下载助手、视频时间戳采集助手、评测雷达·Excel 异常检测。
- 每个插件均依据扩展管理页信息补全产品定位，并分别提供“它是什么、解决什么问题、如何使用”三个内容模块和三步操作流程。
- 总览采用五张极简玻璃卡片：包含字形图标、类型、版本号与一句话价值，隐藏无助于理解的扩展 ID。
- 卡片加入随指针移动的局部柔光、轻微 3D 倾斜、顶部流光、轨道圆环与错峰入场动画；减少装饰密度，保持 INS 与 Apple 广告式克制感。
- 详情页使用左右分栏与差速旋转轨道；移动端自动切换为单列长页，信息层级、间距和滚动阅读均保持完整。
- 浏览器逐项实测五个插件详情均包含 3 个说明模块和 3 个使用步骤；桌面与 `390×844` 移动端均正常，控制台无站点错误。
